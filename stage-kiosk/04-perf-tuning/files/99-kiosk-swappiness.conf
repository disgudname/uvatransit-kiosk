# Default (60) swaps fairly eagerly, which on this device means visible jank
# whenever a swapped-out page has to come back in. Lower priority avoids
# swapping until memory pressure is real, at the cost of some page-cache
# efficiency - an easy trade given this device does one fixed job.
vm.swappiness=10
